---
const cars = [
  {
    name: "Mobil Manual",
    description: "Cocok untuk yang ingin menguasai teknik mengemudi dasar dengan kontrol penuh",
    image: "https://images.unsplash.com/photo-1533473359331-0135ef1b58bf?w=800&h=600&fit=crop"
  },
  {
    name: "Mobil Matic",
    description: "Pilihan tepat untuk pemula yang ingin belajar dengan lebih mudah dan nyaman",
    image: "https://images.unsplash.com/photo-1552519507-da3b142c6e3d?w=800&h=600&fit=crop"
  },
  {
    name: "Mobil Terawat",
    description: "Semua mobil NASYA dalam kondisi prima, aman, dan nyaman untuk belajar",
    image: "https://images.unsplash.com/photo-1619767886558-efdc259cde1a?w=800&h=600&fit=crop"
  }
];
---

<section class="py-16 sm:py-20 lg:py-24 bg-accent-cream/30">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Section Header -->
    <div class="text-center mb-10 sm:mb-16 reveal">
      <span class="section-label">Armada Kami</span>
      <h2 class="mt-3 section-title">
        Mobil<br /><span class="text-primary">Latihan</span>
      </h2>
      <p class="mt-4 section-subtitle mx-auto">
        Semua mobil latihan terawat dengan baik, nyaman, dan aman untuk memastikan pengalaman belajar yang menyenangkan
      </p>
    </div>
    
    <!-- Cars Grid -->
    <div class="grid md:grid-cols-3 gap-8">
      {cars.map((car, index) => (
        <div class={`group bg-white rounded-[32px] overflow-hidden shadow-sm hover:shadow-xl transition-all duration-300 hover:-translate-y-2 reveal stagger-${index + 1}`}>
          <div class="aspect-[4/3] overflow-hidden relative bg-gray-100">
            <!-- Shimmer placeholder -->
            <div class="absolute inset-0 shimmer-bg"></div>
            <img 
              src={car.image}
              alt={car.name}
              loading="lazy"
              decoding="async"
              class="w-full h-full object-cover group-hover:scale-105 transition-all duration-500 opacity-0"
              onload="this.classList.remove('opacity-0'); this.previousElementSibling.style.display='none';"
            />
          </div>
          <div class="p-6">
            <h3 class="text-xl font-bold text-text-primary mb-2">
              {car.name}
            </h3>
            <p class="text-text-secondary">
              {car.description}
            </p>
          </div>
        </div>
      ))}
    </div>
  </div>
</section>

<style>
  .shimmer-bg {
    background: linear-gradient(90deg, #f0f0f0 0%, #e0e0e0 50%, #f0f0f0 100%);
    background-size: 200% 100%;
    animation: shimmer 1.5s ease-in-out infinite;
  }
  
  @keyframes shimmer {
    0% { background-position: 200% 0; }
    100% { background-position: -200% 0; }
  }
  
  @media (prefers-reduced-motion: reduce) {
    .shimmer-bg { animation: none; background: #f0f0f0; }
  }
</style>
